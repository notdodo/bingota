name: Infrastructure Security Scan
on:
  push:
    branches:
      - main
    paths-ignore:
      - "**.rs"
      - "**.md"
      - "Cargo.*"
  pull_request:
    branches:
      - main
    paths-ignore:
      - "**.rs"
      - "**.md"
      - "Cargo.*"

concurrency:
  group: bingotà-kics-${{ github.ref }}
  cancel-in-progress: true

jobs:
  infra-security-scan:
    uses: notdodo/github-actions/.github/workflows/infra-security-scan.yml@140ee038bc8d88d63e8df8f29ebfe0c5e98112e6
    # v0.1.0
