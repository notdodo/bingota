name: Infrastructure Security Scan
on:
  push:
    branches:
      - main
    paths-ignore:
      - "**.rs"
      - "**.md"
      - "Cargo.*"
  pull_request:
    branches:
      - main
    paths-ignore:
      - "**.rs"
      - "**.md"
      - "Cargo.*"

concurrency:
  group: bingotà-kics-${{ github.ref }}
  cancel-in-progress: true

jobs:
  infra-security-scan:
    uses: notdodo/github-actions/.github/workflows/infra-security-scan.yml@0bd5ee314c44d05c9c871ae67b47487c89fb772c
    # infra-scan-v0.0.1
