name: Infrastructure Security Scan
on:
  push:
    branches:
      - main
    paths-ignore:
      - "**.rs"
      - "**.md"
      - "Cargo.*"
  pull_request:
    branches:
      - main
    paths-ignore:
      - "**.rs"
      - "**.md"
      - "Cargo.*"

concurrency:
  group: bingotà-kics-${{ github.ref }}
  cancel-in-progress: true

jobs:
  infra-security-scan:
    uses: notdodo/github-actions/.github/workflows/infra-security-scan.yml@b07e5cc4e96f06b8e34e1d832f898fce6e6512fe
    # infra-scan-v0.0.0
